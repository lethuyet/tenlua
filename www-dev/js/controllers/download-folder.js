'use strict';

angular.module( 'TenLua' )
	.controller( 'DownloadFolderCtrl', function( $rootScope, $scope, $http, $location, $routeParams, $interval, $q, $modal, uiState, filesSrv, API_URL, AUTH_EVENTS ) {

		uiState.page.setTitle('Download folder');

	



	} );
